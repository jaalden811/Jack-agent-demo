# Static assets

Place the attached Jack image from the Cursor chat at:

```text
public/jack.jpg
```

The main landing page references `/jack.jpg` as the first visual element. No binary image asset was available in the repository for this agent run, so this TODO records the required placement path without storing or fabricating an image.
